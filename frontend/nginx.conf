server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: React Router'ın client-side yönlendirdiği yollar (örn.
    # /transactions/5) fiziksel dosya değil — hepsi index.html'e düşer.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
